import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.todos',
  appName: 'My Todos',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
